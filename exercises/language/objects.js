/**
 * Person should be an object with 3 properties:
 *   - name 
 *   - age
 *   - city
 * 
 * Additionally, it should have an instance method that returns a greeting. In other words,
 * we should be able to call person.greeting() and it return a greeting string.
 */

const person = undefined

const veggies = {
  lettuce: 5,
  tomatoes: 3,
  oyster: 15,
}

const meats = {
  bacon: 2,
  sausage: 3,
  oyster: 10,
}

/**
 * The value of inventory should be the merge of the veggies and meats objects above
 * such that we contain a new inventory object that has the properties of both. 
 * 
 * Note that both veggies.oyster and meats.oyster exists. In case of overlap, we use
 * the veggie count rather than the meat count.
 */

const inventory = undefined

module.exports = { person, inventory }